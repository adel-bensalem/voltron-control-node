- name: Deploy application
  hosts: webservers
  remote_user: git
  become: yes
  roles:
    - deploy-application

- name: Save application location
  hosts: databases
  remote_user: john
  become: yes
  roles:
    - save-application-location
  vars:
    application_port: "{{ port_map[application_name] }}"
